anthropic>=0.40.0
httpx>=0.27.0
resend>=2.0.0
pypdf>=5.0.0
# pdfminer.six is the fallback CV extractor. pypdf is fast and
# bomb-resistant but silently mis-handles a long tail of real PDFs
# (LaTeX, Pages, InDesign, custom-encoded fonts); pdfminer recovers
# text from those. See core/pdf.extract_pdf_text.
pdfminer.six>=20240706
weasyprint>=68.0
python-dotenv>=1.0.0
supabase>=2.0.0
pyyaml>=6.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
# ATS integration (PR2). MergeAPIClient is the official Python SDK for the
# Merge Unified API. cryptography backs AES-GCM encryption of per-org Merge
# linked-account tokens at rest.
MergePythonClient>=2.0.0
cryptography>=43.0.0
